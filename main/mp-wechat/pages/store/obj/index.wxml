<wxs module="wxs">
    module.exports = {
        stringify: function (obj) {
            return JSON.stringify(obj);
        }
    };
</wxs>

<view>
    <view>{{wxs.stringify(obj)}}</view>
    <view>
        <button bind:tap="editArrEl">修改数组元素</button>
    </view>
    <view>
        <button bind:tap="editObjField">修改字段</button>
    </view>
</view>