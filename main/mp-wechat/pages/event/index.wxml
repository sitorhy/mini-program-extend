<component-bus>
    <view wx:for="{{keys}}" wx:key="item">
        <component>
            <component2 wx:if="{{index%2===0}}"/>
        </component>
    </view>
</component-bus>
<component-bus>

</component-bus>

<button type="primary" bind:tap="checkLevel">Parent Level</button>
<button type="warn" bind:tap="checkChildren">Children</button>